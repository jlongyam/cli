import { args } from './args.js';
import { input } from './input.js';
/**
 * Exports the module's main CLI argument parser and input utilities.
 *
 * @module
 * @exports args - Parsed CLI arguments using the internal parser.
 * @exports input - Input utility functions for user prompts.
 * @example
 * import { args } from "@jlongyam/cli";
 * console.log(args);
 */
export {
  args,
  input
}